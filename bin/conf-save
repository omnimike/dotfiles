#!/bin/bash
SOURCE_FILE="${BASH_SOURCE}"
if [ -L $SOURCE_FILE ]; then
    SOURCE_FILE="$( readlink $SOURCE_FILE )"
fi
DIR="$( cd "$( dirname "$SOURCE_FILE" )" && pwd )"
git -C $DIR add -f . && \
    git -C $DIR commit -m "save" && \
    git -C $DIR push
